"use strict";
(() => {
    // Void se refiere a funciones que no retorna nada
    function callBatman() {
    }
    //como hacerlo en una funcion de flecha
    const callSuperman = () => {
    };
    const a = callBatman();
    console.log(a);
})();
